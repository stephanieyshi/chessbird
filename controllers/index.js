var routes = require('./routes.js');
var tweets = require('./tweet.js');

routes.app.listen(3000);
console.log("Listening on localhost:3000");

// to make a tweet
twitter.postTweet(convertChessState(arr), error, success);
